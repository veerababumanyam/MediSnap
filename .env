# Frontend: No central Gemini key — users provide their own via Settings UI.
# Backend (medisnap agent): Set GOOGLE_API_KEY in medisnap/.env for server-side agent.

# Firebase API Configuration (from .env.local usually, but listed here for reference)
# VITE_FIREBASE_API_KEY=...
